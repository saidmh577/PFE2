<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.pfe2">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PFE2">
        <activity android:name=".ResultatDeformation"></activity>
        <activity android:name=".MotPassOublie" />
        <activity android:name=".Inscription" />
        <activity android:name=".VerificationDeformation" />
        <activity android:name=".ResultatQuantiteArmature" />
        <activity android:name=".QuantiteArmature" />
        <activity android:name=".ArmatureActivity" />
        <activity android:name=".MainHome" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>